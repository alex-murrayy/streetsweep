# Core computer vision and image processing
opencv-python>=4.8.0
numpy>=1.21.0
Pillow>=9.0.0

# Machine learning and deep learning frameworks
# Note: TensorFlow compatibility varies by Python version
# For Python 3.13, use tf-nightly (stable TensorFlow doesn't support 3.13 yet)
tf-nightly>=2.21.0.dev20251013; python_version >= "3.13"
tensorflow>=2.13.0; python_version < "3.13" and (platform_machine != "arm64" or platform_system != "Darwin")
tensorflow-macos>=2.13.0; python_version < "3.13" and platform_machine == "arm64" and platform_system == "Darwin"
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# Additional ML libraries
scikit-learn>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Data handling and utilities
pandas>=1.5.0
scipy>=1.9.0
tqdm>=4.64.0

# Video processing
imageio>=2.25.0
imageio-ffmpeg>=0.4.8

# Web dashboard dependencies (for future integration)
flask>=2.3.0
flask-socketio>=5.3.0
websockets>=11.0.0

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0

# Optional: GPU acceleration
# Uncomment if you have CUDA-compatible GPU
# tensorflow-gpu>=2.13.0
# torch-audio>=2.0.0

# Optional: Advanced computer vision
# albumentations>=1.3.0  # Data augmentation
# imgaug>=0.4.0          # Image augmentation

# ROS integration (Linux/Ubuntu only - install separately)
# Note: ROS packages are not available via pip and must be installed through apt
# Install ROS Noetic following official instructions:
# http://wiki.ros.org/noetic/Installation/Ubuntu
# 
# Required ROS packages (install with apt):
# sudo apt install ros-noetic-rospy
# sudo apt install ros-noetic-cv-bridge
# sudo apt install ros-noetic-sensor-msgs
# sudo apt install ros-noetic-geometry-msgs
# 
# Or install via pip for development (limited functionality):
# rospkg>=1.3.0
# rospy>=1.14.0; platform_system == "Linux"
# cv-bridge>=1.15.0; platform_system == "Linux"
